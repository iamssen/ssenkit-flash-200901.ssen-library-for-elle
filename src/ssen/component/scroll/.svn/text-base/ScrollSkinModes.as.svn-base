package ssen.component.scroll 
{
	import ssen.core.display.SkinMode;		
	/**
	 * @author SSen
	 */
	public class ScrollSkinModes 
	{
		public static const TRACK : Array = [SkinMode.DEFAULT, SkinMode.OVER, SkinMode.ACTION, SkinMode.DISABLE];
		public static const THUMB : Array = [SkinMode.DEFAULT, SkinMode.ACTION, SkinMode.DISABLE];
	}
}
